# i18n — English

Key→text mapping for report output when `lang=en`. SKILL.md and workflows must read this file and substitute keys into output. Do NOT hardcode English strings in logic.

## Header

| Key | Text |
|---|---|
| `report_title` | vbsec Security Scan Report |
| `header_scope` | Scope |
| `header_files` | Files |
| `header_primary_lang` | Primary language |
| `header_mode` | Mode |
| `header_date` | Scan date |
| `header_lang` | Report language |
| `header_using_generic` | using generic rules |
| `header_using_specialized` | using specialized rules |
| `header_no_specialized` | no specialized rules available |

## Scope labels

| Key | Text |
|---|---|
| `scope_uncommitted` | Uncommitted changes |
| `scope_staged` | Staged files only |
| `scope_commit_within` | Commits within last {days} days |
| `scope_commit_id` | Commit {sha} |
| `scope_pr` | Pull Request #{number} |
| `scope_all` | Entire repo |

## Mode

| Key | Text |
|---|---|
| `mode_small` | SMALL (inline scan) |
| `mode_large` | LARGE (sub-agent delegation) |

## Verdict

| Key | Text |
|---|---|
| `verdict_label` | VERDICT |
| `verdict_pass` | PASS |
| `verdict_warn` | PASS WITH WARNINGS |
| `verdict_fail` | FAIL |
| `verdict_pass_desc` | No critical issues found. Safe to deploy. |
| `verdict_warn_desc` | HIGH-severity issues found. Address before production. |
| `verdict_fail_desc` | CRITICAL issues found. DO NOT deploy until fixed. |

## Severity

| Key | Text |
|---|---|
| `severity_critical` | CRITICAL |
| `severity_high` | HIGH |
| `severity_medium` | MEDIUM |
| `severity_low` | LOW |
| `severity_critical_short` | Critical |
| `severity_high_short` | High |
| `severity_medium_short` | Medium |
| `severity_low_short` | Low |

## Section headers

| Key | Text |
|---|---|
| `section_critical` | CRITICAL (blocks deploy) |
| `section_high` | HIGH (must fix) |
| `section_medium` | MEDIUM |
| `section_low` | LOW |
| `section_passed` | PASSED CHECKS |
| `section_warnings` | WARNINGS |
| `section_summary` | Summary |
| `section_recommendations` | Recommendations |

## Table headers

| Key | Text |
|---|---|
| `col_file_line` | File:Line |
| `col_rule` | Rule |
| `col_issue` | Issue |
| `col_fix` | Fix |
| `col_severity` | Severity |
| `col_recommendation` | Recommendation |
| `col_count` | Count |
| `col_status` | Status |

## Counts & summary

| Key | Text |
|---|---|
| `count_files_reviewed` | Files reviewed |
| `count_rules_applied` | Rules applied |
| `count_findings` | Total findings |
| `count_critical` | Critical issues |
| `count_high` | High issues |
| `count_medium` | Medium issues |
| `count_low` | Low issues |
| `no_findings` | No issues found |
| `all_passed` | All checks passed |

## Common phrases (for fix recommendations — rule files can template these)

| Key | Text |
|---|---|
| `phrase_use_parameterized` | Use parameterized query / prepared statement |
| `phrase_never_concat` | Never concatenate untrusted input into queries |
| `phrase_use_env_var` | Move to environment variable; do not commit secrets |
| `phrase_rotate_now` | Rotate the key immediately (if it was pushed to Git) |
| `phrase_add_gitignore` | Add `.env` to `.gitignore` |
| `phrase_validate_input` | Validate and sanitize input before use |
| `phrase_use_bcrypt` | Use bcrypt/argon2/scrypt for password hashing |
| `phrase_use_csrf_token` | Add CSRF token for state-changing endpoints |
| `phrase_specific_origin` | Set a specific origin instead of `*` |
| `phrase_use_transaction` | Wrap in a transaction with row-level lock to prevent races |
| `phrase_check_authz` | Add backend authorization check; never trust the frontend |
| `phrase_disable_debug_prod` | Disable `DEBUG=true` in production |
| `phrase_update_dep` | Update the dependency to a CVE-patched version |
| `phrase_use_safe_yaml` | Use `yaml.safe_load` (Python) or equivalent |
| `phrase_no_shell_true` | Do not use `shell=True` with user input |
| `phrase_whitelist_redirect` | Whitelist redirect destination domains |

## System messages

| Key | Text |
|---|---|
| `msg_no_git` | Not a git repository. Please `cd` into a git repo first. |
| `msg_no_files` | No files in scan scope. |
| `msg_routing_small` | SMALL mode: inline scan |
| `msg_routing_large` | LARGE mode: chunking into {n} groups, spawning sub-agents |
| `msg_scanning` | Scanning... |
| `msg_done` | Scan complete |
| `msg_invalid_lang` | Invalid `lang` parameter. Use `vi` or `en`. Default: `vi`. |

## Footer

| Key | Text |
|---|---|
| `footer_generated_by` | Generated by |
| `footer_repo` | https://github.com/tanviet12/vbsec |
| `footer_disclaimer` | This report is a reference — not a substitute for professional security audit. |
| `footer_next_steps` | Next steps |
| `footer_next_steps_text` | Fix CRITICAL issues first. Re-scan to confirm. |

## Verbose finding blocks (v0.3+)

| Key | Text |
|---|---|
| `header_short_description` | Short description |
| `header_why_dangerous` | Why is this dangerous? |
| `header_attack_scenario` | How does an attacker exploit this? |
| `header_current_code` | Current code (UNSAFE) |
| `header_safe_code` | Safe code (replace with) |
| `header_impact` | Impact |
| `header_fix_code` | Fix |
| `header_read_more` | Read more |
| `header_overview_table` | Overview (details below) |
| `phrase_input_untrusted` | input from user — UNTRUSTED |
| `phrase_concat_to_sink` | concatenated directly into |

## Persistence (v0.3+)

| Key | Text |
|---|---|
| `msg_report_saved` | Report saved to |
| `msg_gitignore_warning_title` | Recommendation |
| `msg_gitignore_warning_text` | The `vbsec-reports/` directory is not listed in `.gitignore`. To avoid committing scan reports to Git, add `vbsec-reports/` to `.gitignore`. |
| `msg_reports_dir_created` | Created `vbsec-reports/` directory for persisted reports |
