/**
 * Semver for the MCP tool JSON contract (viz and future surfaces).
 * Must match `schemas/tool-contract-version.json`, `contracts/tool-contract-version.json`,
 * and `src/data360/tool_contract_version.json` in the data360-mcp repo.
 */
export const TOOL_CONTRACT_VERSION = "1.0.0" as const;
