# OECD MCP Server - Environment Variables Template
# Copy this file to .env for local development
# DO NOT commit .env to version control

# ========== Application Configuration ==========

# Node.js environment (development, production, test)
NODE_ENV=production

# HTTP server port
PORT=3000

# ========== Node.js Configuration ==========

# Memory limit for Node.js heap (in MB)
# Free tier (512MB total): Use 450MB
# Production (1GB total): Use 768MB
# Enterprise (2GB+ total): Use 1536MB
NODE_OPTIONS=--max-old-space-size=512

# ========== Logging Configuration ==========

# Log level (error, warn, info, debug, trace)
LOG_LEVEL=info

# Log format (json, pretty)
LOG_FORMAT=json

# ========== Monitoring Configuration (Optional) ==========

# Enable Prometheus metrics endpoint
# ENABLE_METRICS=true

# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ========== OECD API Configuration ==========

# OECD SDMX API base URL (default: https://sdmx.oecd.org/public/rest)
# OECD_API_BASE_URL=https://sdmx.oecd.org/public/rest

# OECD API timeout in milliseconds (default: 30000)
# OECD_API_TIMEOUT=30000

# ========== Rate Limiting (Optional) ==========

# Enable rate limiting
# ENABLE_RATE_LIMIT=false

# Rate limit window in milliseconds (default: 60000 = 1 minute)
# RATE_LIMIT_WINDOW=60000

# Max requests per window
# RATE_LIMIT_MAX_REQUESTS=100

# ========== CORS Configuration ==========

# Allowed origins (default: * for public API)
# CORS_ORIGINS=*

# ========== Cache Configuration (Future) ==========

# Enable caching (if implemented)
# ENABLE_CACHE=false

# Cache TTL in seconds
# CACHE_TTL=3600
